# Micro Framework Module Definition

name: ufw

cmake-modules: cmake/modules
cmake-toolchains: cmake/toolchains

cmake-extensions:
  ufw:
    include: setup_ufw
    module: SetupUFW
  libtap:
    include: add_libtap
    module: Libtap

dependencies:
  - name: libtap
    revision: master
