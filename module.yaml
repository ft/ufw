# Micro Framework Module Definition

name: ufw

cmake-modules: cmake/modules
cmake-toolchains: cmake/toolchains

cmake-extensions:
  ufw:
    include: setup_ufw
    module: SetupUFW
  arm-cmsis:
    include: add_arm_cmsis
    module: ArmCmsis
  freertos-system:
    include: add_freertos
    module: FreeRTOS
  libtap:
    include: add_libtap(${moduleroot})
    module: Libtap

dependencies:
  - name: libtap
    revision: master
